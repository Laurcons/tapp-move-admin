import { InitialismPipe } from './initialism.pipe';

describe('InitialismPipe', () => {
  it('create an instance', () => {
    const pipe = new InitialismPipe();
    expect(pipe).toBeTruthy();
  });
});
